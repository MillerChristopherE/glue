datasource_empty
{
    eachLine = { set, callback ->
    }
    
    transfer = { from, to ->
    }
    
    clean = { set ->
    }
    
    dryrunAllowWrite = { from, to ->
        return true
    }
    
    dryrunAllowRead = { from ->
        return true
    }
}


// Predefined data source named empty for convenience.
empty = {
    type = "empty"
}

